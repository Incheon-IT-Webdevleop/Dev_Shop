<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shop.purchase.PurchaseMapper">

    <!--매입 목록 조회 -->
    <select id="selectAll" resultType="com.shop.shop.purchase.PurchaseDto">
        select * from purchaseInfo order by purchaseDate desc
    </select>






</mapper>